import React, { useState } from "react";

function AdminTimerBox() {
  const [inputSecond, setInputSecond] = useState(600); // 입력 초기값

  return (
    <>
      <div>
        <button>버튼</button>
      </div>
    </>
  );
}
